 <%- #form_with(model: item, local: true) do |form| %>
<%= form_for([@warehouse, item]) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :item_name, class: "form-control width20" %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :item_price, :step => 0.01, class: "form-control width20" %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :item_quantity, class: "form-control width20" %>
  </div>

  <div class="actions">
    <%= form.submit :class => "btn btn-success"%>
  </div>
<% end %>
